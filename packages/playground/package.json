{
    "name": "playground",
    "version": "0.1.0",
    "scripts": {
        "dev": "bun --preload ./src/vue-plugin.ts --hot src/main.ts",
        "build": "bun run sync && bun run ./scripts/build.ts",
        "sync": "bun run ./scripts/sync.ts"
    },
    "dependencies": {
        "@types/bun": "^1.3.13",
        "@vue/reactivity": "^3.5.33",
        "compiler": "workspace:*",
        "core": "workspace:*"
    },
    "devDependencies": {
        "native": "workspace:*"
    }
}
